export interface Shelter {
  id: string
  profileId: string
  name: string
  description?: string
  address?: string
  latitude?: number
  longitude?: number
  phone?: string
  logoUrl?: string
  createdAt: string
}
